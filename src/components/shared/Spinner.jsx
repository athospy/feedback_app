import spinner from '../assets/spinner.gif'

function Spinner() {
  return (
    <img src={spinner} alt='Loading...' style={{with: '100%', margin: 'auto', display: 'block'}}>
      
    </img>
  )
}

export default Spinner
